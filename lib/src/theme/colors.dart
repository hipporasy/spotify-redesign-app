import 'package:flutter/material.dart';

extension AppColor on Color {
  static const Color primary = Color(0xFF00954d);
  static const Color primaryBackground = Color(0xFF00954d);
  static const Color darkText = Color(0xFF222C40);
  static const Color lightText = Color(0xFF797979);
}
